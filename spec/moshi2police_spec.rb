require "spec_helper"

RSpec.describe Moshi2police do
  it "has a version number" do
    expect(Moshi2police::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
